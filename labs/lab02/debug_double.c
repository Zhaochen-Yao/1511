/*
    Enter a number, double it.
*/

#include <stdio.h>

int main(void) {
    int x;
    
    printf("Enter a number: ");
    scanf("%d", &x);
    
    printf("Doubled: %d x 2 = %d\n", x, x*2);

    return 0;
}

    /*
    // Read in the number:
    int x;
    print("Enter a number: ");
    scan("%d", x);

    // Doubled the value of x and assign it to y.
    int y;
    x + x = y;

    print("Doubled: %d x 2 = %d\n", x, y);
    */